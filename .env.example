# 백엔드 API 베이스 URL
# MSW 목업으로 개발할 때는 /api (같은 origin)로 둬야 서비스워커가 요청을 가로챈다.
# 실제 백엔드에 직접 붙을 때만 http://localhost:8080/api 같은 절대 URL로 바꾼다.
VITE_API_BASE_URL=/api

# MSW 목업 서버 활성화 여부 (백엔드 미준비 시 true)
VITE_ENABLE_MSW=true
